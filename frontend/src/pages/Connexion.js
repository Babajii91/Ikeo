import React from "react";
import Layout from "../components/Layout/Layout";

const Connexion = () => {
    return(
        <Layout>
            <h1>Connexion</h1>
        </Layout>
    );
};

export default Connexion;